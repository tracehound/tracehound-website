---
import { ArrowRight, Terminal } from 'lucide-react'
---

<section class="relative overflow-hidden pt-32 pb-24 border-b border-surface-highlight">
  <!-- Background effects -->
  <div class="absolute inset-0 z-0 bg-grid-pattern opacity-[0.4] mask-image-gradient"></div>

  <!-- Content -->
  <div class="container-custom relative z-10">
    <div class="max-w-4xl">
      <div
        class="inline-flex items-center gap-2 px-3 py-1 mb-8 border border-primary/30 bg-primary/5 text-primary text-xs font-mono tracking-wider uppercase"
      >
        <span class="w-2 h-2 bg-primary animate-pulse"></span>
        System Operational
      </div>

      <h1 class="text-5xl md:text-7xl font-bold tracking-tighter mb-8 leading-tight">
        The deterministic <br />
        <span class="text-primary">security buffer</span> for AI.
      </h1>

      <p class="text-xl md:text-2xl text-text-muted mb-12 leading-relaxed max-w-2xl font-light">
        Tracehound isolates your application from LLM failures. It provides a <span
          class="text-white font-medium">frozen, crash-proof runtime</span
        > that enforces security policies with zero variance.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 mb-16">
        <a
          href="/docs/getting-started"
          class="inline-flex items-center justify-center px-8 py-4 text-base font-medium text-background bg-primary hover:bg-primary/90 transition-all rounded-sm group"
        >
          Initialize Runtime
          <ArrowRight className="ml-2 h-4 w-4 group-hover:translate-x-1 transition-transform" />
        </a>
        <a
          href="https://github.com/laphilosophia/tracehound"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center justify-center px-8 py-4 text-base font-medium text-text border border-surface-highlight hover:border-primary/50 hover:bg-surface-highlight/50 transition-all rounded-sm font-mono"
        >
          <Terminal className="mr-2 h-4 w-4 text-text-muted" />
          npm install tracehound
        </a>
      </div>

      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 border-t border-surface-highlight pt-8">
        <div>
          <div class="text-2xl font-bold font-mono text-white mb-1">0ms</div>
          <div class="text-sm text-text-muted font-mono uppercase tracking-wider">GC Pause</div>
        </div>
        <div>
          <div class="text-2xl font-bold font-mono text-white mb-1">100%</div>
          <div class="text-sm text-text-muted font-mono uppercase tracking-wider">Determinism</div>
        </div>
        <div>
          <div class="text-2xl font-bold font-mono text-white mb-1">ISO</div>
          <div class="text-sm text-text-muted font-mono uppercase tracking-wider">Compliant</div>
        </div>
        <div>
          <div class="text-2xl font-bold font-mono text-white mb-1">0</div>
          <div class="text-sm text-text-muted font-mono uppercase tracking-wider">Dependencies</div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .mask-image-gradient {
    mask-image: linear-gradient(to bottom, black 40%, transparent 100%);
    -webkit-mask-image: linear-gradient(to bottom, black 40%, transparent 100%);
  }
</style>
