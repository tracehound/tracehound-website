---
import { ArrowRight } from 'lucide-react'
import CyberBackground from './CyberBackground'
---

<section
  class="relative overflow-hidden pt-32 pb-24 border-b border-dashed border-surface-highlight"
>
  <!-- Animated Cyber Background -->
  <CyberBackground client:load />

  <!-- Static grid overlay -->
  <div class="absolute inset-0 z-0 bg-grid-pattern opacity-[0.2] mask-image-gradient"></div>

  <!-- Content -->
  <div class="container-custom relative w-full z-10">
    <div
      class="inline-flex items-center gap-2 px-3 py-1 mb-8 border border-dashed border-primary/30 bg-primary/5 text-primary text-xs font-mono tracking-wider uppercase"
    >
      <span class="w-2 h-2 bg-primary animate-pulse"></span>
      System Operational
    </div>

    <h1 class="text-4xl md:text-6xl font-bold tracking-tighter mb-8 leading-tight">
      The deterministic<br />
      <span class="text-primary">security buffer</span><br /> for modern applications.
    </h1>

    <p class="text-lg md:text-xl text-text-muted mb-12 leading-relaxed max-w-2xl font-light">
      Tracehound actively contains security-relevant runtime anomalies in a bounded, deterministic
      layer. It isolates, rate-limits, and quarantines threats <span class="text-white font-medium"
        >— without making security decisions.</span
      >
    </p>

    <div class="flex flex-col sm:flex-row gap-4 mb-16">
      <a
        href="/docs/quickstart"
        class="inline-flex items-center justify-center px-6 py-3 text-base font-medium text-background bg-primary hover:bg-primary/90 transition-all rounded-sm group"
      >
        Initialize Runtime
        <ArrowRight className="ml-2 h-4 w-4 group-hover:translate-x-1 transition-transform" />
      </a>
    </div>
  </div>

  <div class="w-full border-t border-dashed border-surface-highlight pt-8">
    <div class="container-custom grid grid-cols-2 md:grid-cols-4 gap-8">
      <div>
        <div class="text-2xl font-bold font-mono text-white mb-1">Zero-Allocation</div>
        <div class="text-sm text-text-muted font-mono uppercase tracking-wider">Hot Path</div>
      </div>
      <div>
        <div class="text-2xl font-bold font-mono text-white mb-1">100%</div>
        <div class="text-sm text-text-muted font-mono uppercase tracking-wider">Determinism</div>
      </div>
      <div>
        <div class="text-2xl font-bold font-mono text-white mb-1">ISO</div>
        <div class="text-sm text-text-muted font-mono uppercase tracking-wider">Compliant</div>
      </div>
      <div>
        <div class="text-2xl font-bold font-mono text-white mb-1">0</div>
        <div class="text-sm text-text-muted font-mono uppercase tracking-wider">Dependencies</div>
      </div>
    </div>
  </div>
</section>

<style>
  .mask-image-gradient {
    mask-image: linear-gradient(to bottom, black 40%, transparent 100%);
    -webkit-mask-image: linear-gradient(to bottom, black 40%, transparent 100%);
  }
</style>
