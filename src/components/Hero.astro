---
import { ArrowRight } from 'lucide-react'
---

<section class="relative overflow-hidden pt-24 pb-16 md:pt-32 md:pb-24">
  <!-- Background effects -->
  <div class="absolute inset-0 z-0 bg-grid-pattern opacity-[0.2] mask-image-gradient"></div>
  <div
    class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-primary/20 rounded-full blur-[120px] pointer-events-none"
  >
  </div>

  <div class="container-custom relative z-10 flex flex-col items-center text-center">
    <div
      class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-primary/30 bg-primary/10 text-primary text-xs font-mono mb-8 animate-fade-in-up"
    >
      <span class="relative flex h-2 w-2">
        <span
          class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75"
        ></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-primary"></span>
      </span>
      v1.0.0 Now Available
    </div>

    <h1
      class="text-5xl md:text-7xl font-bold tracking-tighter mb-6 bg-clip-text text-transparent bg-gradient-to-b from-white to-white/60"
    >
      Deterministic Security. <br class="hidden md:block" />
      <span class="text-primary">Zero Guesswork</span>.
    </h1>

    <p class="text-xl text-text-muted mb-10 max-w-2xl leading-relaxed">
      A deterministic security buffer for your FinTech applications. Tracehound quarantines threats
      and creates immutable evidence
      <strong>without</strong> risking false positives.
    </p>

    <div class="flex flex-col sm:flex-row gap-4 w-full sm:w-auto">
      <a href="/docs/getting-started" class="btn-primary group">
        Get Started
        <ArrowRight className="ml-2 h-4 w-4 group-hover:translate-x-1 transition-transform" />
      </a>
      <a href="https://github.com/tracehound/core" target="_blank" class="btn-secondary">
        View on GitHub
      </a>
    </div>

    <!-- Stats / Trust Indicators -->
    <div
      class="mt-16 grid grid-cols-2 md:grid-cols-4 gap-8 md:gap-16 border-t border-surface-highlight pt-8"
    >
      <div class="flex flex-col items-center">
        <span class="text-2xl font-mono font-bold text-white">0ms</span>
        <span class="text-sm text-text-muted">Decision Latency</span>
      </div>
      <div class="flex flex-col items-center">
        <span class="text-2xl font-mono font-bold text-white">SHA-256</span>
        <span class="text-sm text-text-muted">Evidence Integrity</span>
      </div>
      <div class="flex flex-col items-center">
        <span class="text-2xl font-mono font-bold text-white">100%</span>
        <span class="text-sm text-text-muted">Deterministic</span>
      </div>
      <div class="flex flex-col items-center">
        <span class="text-2xl font-mono font-bold text-white">Enterprise</span>
        <span class="text-sm text-text-muted">Production Ready</span>
      </div>
    </div>
  </div>
</section>

<style>
  .mask-image-gradient {
    mask-image: radial-gradient(circle at center, black, transparent 80%);
  }
</style>
