---
import { Activity, ArrowRight, Eye, Zap } from 'lucide-react'
---

<section
  class="py-24 border-y border-dashed border-purple-500/20 bg-gradient-to-b from-purple-500/5 to-transparent"
>
  <div class="container-custom">
    <div class="flex flex-col lg:flex-row items-center gap-12 lg:gap-16">
      <!-- Text Side -->
      <div class="flex-1 space-y-6">
        <div
          class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full border border-yellow-500/30 bg-yellow-500/10 text-yellow-400 text-xs font-medium"
        >
          <span class="w-2 h-2 bg-yellow-400 rounded-full animate-pulse"></span>
          In Development
        </div>

        <h2 class="text-3xl md:text-5xl font-bold tracking-tight leading-tight">
          Meet <span class="text-purple-400">Argos</span>,<br />
          your runtime watchdog.
        </h2>

        <p class="text-xl text-text-muted leading-relaxed max-w-xl">
          Know <em class="text-white font-medium not-italic">when</em> your app starts misbehaving — before
          your users do. Event loop lag, memory spikes, unexpected slowdowns. Argos catches the drift.
        </p>

        <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 pt-4">
          <div class="flex items-center gap-3 text-sm">
            <div
              class="w-8 h-8 rounded-lg bg-purple-500/10 border border-purple-500/20 flex items-center justify-center text-purple-400"
            >
              <Eye className="w-4 h-4" />
            </div>
            <span class="text-text-muted">Event loop monitoring</span>
          </div>
          <div class="flex items-center gap-3 text-sm">
            <div
              class="w-8 h-8 rounded-lg bg-purple-500/10 border border-purple-500/20 flex items-center justify-center text-purple-400"
            >
              <Activity className="w-4 h-4" />
            </div>
            <span class="text-text-muted">Anomaly detection</span>
          </div>
          <div class="flex items-center gap-3 text-sm">
            <div
              class="w-8 h-8 rounded-lg bg-purple-500/10 border border-purple-500/20 flex items-center justify-center text-purple-400"
            >
              <Zap className="w-4 h-4" />
            </div>
            <span class="text-text-muted">&lt;1% CPU overhead</span>
          </div>
        </div>

        <div class="flex flex-col sm:flex-row gap-4 pt-6">
          <a
            href="/argos"
            class="inline-flex items-center justify-center px-6 py-3 bg-purple-500 hover:bg-purple-400 text-white font-medium rounded-sm transition-all group"
          >
            Learn More
            <ArrowRight className="ml-2 h-4 w-4 group-hover:translate-x-1 transition-transform" />
          </a>
          <a
            href="#pricing"
            class="inline-flex items-center justify-center px-6 py-3 border border-purple-500/30 text-purple-400 hover:bg-purple-500/10 font-medium rounded-sm transition-all"
          >
            See Pricing
          </a>
        </div>
      </div>

      <!-- Visual Side -->
      <div class="flex-1 w-full max-w-lg">
        <div
          class="relative p-6 bg-background/80 backdrop-blur-sm rounded-lg border border-purple-500/20 shadow-[0_0_60px_rgba(168,85,247,0.1)]"
        >
          <!-- Terminal header -->
          <div class="flex items-center gap-2 mb-4 pb-3 border-b border-white/5">
            <div class="w-3 h-3 rounded-full bg-red-500/60"></div>
            <div class="w-3 h-3 rounded-full bg-yellow-500/60"></div>
            <div class="w-3 h-3 rounded-full bg-green-500/60"></div>
            <span class="ml-2 text-xs text-text-muted font-mono">argos status</span>
          </div>

          <div class="space-y-3 font-mono text-sm">
            <div class="flex justify-between items-center text-text-muted">
              <span class="text-purple-400/60">●</span>
              <span class="flex-1 ml-2">Runtime Status</span>
              <span class="text-green-400 font-medium">HEALTHY</span>
            </div>
            <div class="flex justify-between items-center text-text-muted">
              <span class="text-purple-400/60">●</span>
              <span class="flex-1 ml-2">Event Loop Lag</span>
              <span class="text-white">12ms</span>
            </div>
            <div class="flex justify-between items-center text-text-muted">
              <span class="text-purple-400/60">●</span>
              <span class="flex-1 ml-2">Heap Usage</span>
              <span class="text-white">64%</span>
            </div>
            <div class="flex justify-between items-center text-text-muted">
              <span class="text-purple-400/60">●</span>
              <span class="flex-1 ml-2">Sampling Mode</span>
              <span class="text-purple-400">ADAPTIVE</span>
            </div>

            <div
              class="mt-4 p-3 bg-green-500/10 border border-green-500/20 rounded text-green-400 text-xs flex items-center gap-2"
            >
              <span class="w-2 h-2 bg-green-400 rounded-full"></span>
              No anomalies detected in the last 24h
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
