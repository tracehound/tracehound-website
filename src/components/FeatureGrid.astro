---
import { ShieldAlert, Database, Lock, Fingerprint, Clock, Server } from 'lucide-react';

const features = [
  {
    icon: ShieldAlert,
    title: "Threat Quarantine",
    description: "Suspicious requests are isolated immediately. Tracehound acts as a buffer, preventing potential exploits from reaching your core handlers."
  },
  {
    icon: Database,
    title: "Immutable Evidence",
    description: "Every quarantined request is cryptographically signed and stored. Create a tamper-proof audit trail for compliance and forensics."
  },
  {
    icon: Lock,
    title: "Zero Decisions",
    description: "Tracehound never guesses. It relies on deterministic rules or external signals, eliminating the risk of false positives blocking legitimate users."
  },
  {
    icon: Fingerprint,
    title: "Content Signatures",
    description: "Detect replay attacks and identical payloads instantly using content-based hashing and deduplication."
  },
  {
    icon: Clock,
    title: "Priority Eviction",
    description: "Smart buffer management ensures critical evidence is preserved while handling high-volume attacks without memory leaks."
  },
  {
    icon: Server,
    title: "Framework Agnostic",
    description: "Drop into Express, Fastify, or any Node.js framework. It's just a middleware that sits before your business logic."
  }
];
---

<section class="py-24 bg-surface/30">
  <div class="container-custom">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-5xl font-bold mb-6">Why Tracehound?</h2>
      <p class="text-text-muted text-lg max-w-2xl mx-auto">
        Traditional WAFs block requests and lose the context. Tracehound captures the context and lets you analyze it safely.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {features.map((feature) => (
        <div class="p-6 rounded-xl border border-surface-highlight bg-surface/50 hover:bg-surface-highlight/50 transition-colors group">
          <div class="h-12 w-12 rounded-lg bg-primary/10 flex items-center justify-center text-primary mb-6 group-hover:scale-110 transition-transform duration-300">
            <feature.icon className="h-6 w-6" />
          </div>
          <h3 class="text-xl font-bold mb-3 text-white">{feature.title}</h3>
          <p class="text-text-muted leading-relaxed">
            {feature.description}
          </p>
        </div>
      ))}
    </div>
  </div>
</section>
