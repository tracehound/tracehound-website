---
import { Bell, Clock, Database, Fingerprint, Lock, Snowflake } from 'lucide-react'

const features = [
  {
    icon: Database,
    title: 'Threat Quarantine',
    description:
      'When your WAF flags a threat, we capture and isolate it. Your app stays clean. The evidence stays intact.',
  },
  {
    icon: Fingerprint,
    title: 'Immutable Audit Chain',
    description:
      'Every quarantined request is SHA-256 signed and Merkle-chained. Tamper-proof records for SOC2, HIPAA, and incident response.',
  },
  {
    icon: Lock,
    title: 'Decision-Free by Design',
    description:
      "We never guess. External detectors decide what's a threat — we just quarantine what they flag. Zero false positive risk.",
  },
  {
    icon: Snowflake,
    title: 'Cold Storage Export',
    description:
      'Evidence flows to S3, R2, or GCS automatically. Retention policies you define. No manual archiving.',
  },
  {
    icon: Bell,
    title: 'SIEM Integration',
    description:
      'Push events to Splunk, Elastic, or Datadog. Your SOC sees threats in real-time without polling.',
  },
  {
    icon: Clock,
    title: 'Bounded Memory',
    description:
      'Priority-based eviction ensures critical evidence survives. No memory leaks. No OOM crashes. Ever.',
  },
]
---

<section class="py-24 border-b border-dashed border-surface-highlight">
  <div class="container-custom">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-5xl font-bold mb-6 tracking-tight">Why Tracehound?</h2>
      <p class="text-text-muted text-lg max-w-2xl mx-auto">
        Your WAF blocks threats. We <span class="text-primary font-medium"
          >quarantine and preserve</span
        > the evidence so you can analyze, comply, and prove what happened.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {
        features.map((feature, index) => (
          <div class="p-8 border border-dashed border-surface-highlight bg-surface/30 hover:bg-surface/50 hover:border-primary/30 transition-all group relative">
            <div class="absolute top-4 right-4 text-xs font-mono text-surface-highlight group-hover:text-primary/50 transition-colors">
              0{index + 1}
            </div>
            <div class="h-10 w-10 bg-primary/10 flex items-center justify-center text-primary mb-6 rounded-sm  border-primary/20 group-hover:scale-105 transition-transform duration-300">
              <feature.icon className="h-5 w-5" />
            </div>
            <h3 class="text-lg font-bold mb-3 text-white font-mono uppercase tracking-wide">
              {feature.title}
            </h3>
            <p class="text-text-muted text-sm leading-relaxed">{feature.description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
